import {
  defaultChildrenThemes,
  adjustPalettes
} from "./v5-themes";
const v5SubtlePaletteAdjustments = {
  default: {
    light: (hsl) => ({
      ...hsl,
      s: hsl.s * 0.9
    }),
    dark: (hsl, i) => ({
      ...hsl,
      s: hsl.s * (i <= 4 ? 0.7 : 0.9)
    })
  },
  // yellow palette in radix is especially off from the rest
  yellow: {
    light: (hsl, i) => {
      const t = (i - 1) / 11, sStart = 0.65, sEnd = 0.4, lStart = 1.08;
      return {
        ...hsl,
        s: hsl.s * (sStart + t * (sEnd - sStart)),
        l: hsl.l * (lStart + t * (0.8 - lStart))
      };
    },
    dark: (hsl, i) => ({
      ...hsl,
      s: hsl.s * (i <= 7 ? 0.45 : 0.55),
      l: hsl.l * (i <= 4 ? 0.8 : 1)
    })
  }
}, subtleChildrenThemes = adjustPalettes(defaultChildrenThemes, {
  ...v5SubtlePaletteAdjustments,
  gray: void 0,
  // skip
  neutral: void 0
  // skip
});
export {
  subtleChildrenThemes,
  v5SubtlePaletteAdjustments
};
//# sourceMappingURL=subtleChildrenThemes.js.map
