import React from "react";
import { useMedia } from "./hooks/useMedia";
import { useTheme } from "./hooks/useTheme";
import { jsx } from "react/jsx-runtime";
const _withStableStyle = (Component, styleProvider) => React.forwardRef((props, ref) => {
  const { _expressions = [], ...rest } = props, theme = useTheme(), media = _expressions.some((expr) => typeof expr == "string") ? useMedia() : null, resolvedExpressions = media ? _expressions.map((expr) => typeof expr == "string" ? media[expr] : expr) : _expressions;
  return /* @__PURE__ */ jsx(Component, { ref, style: styleProvider(theme, resolvedExpressions), ...rest });
});
export {
  _withStableStyle
};
//# sourceMappingURL=_withStableStyle.js.map
