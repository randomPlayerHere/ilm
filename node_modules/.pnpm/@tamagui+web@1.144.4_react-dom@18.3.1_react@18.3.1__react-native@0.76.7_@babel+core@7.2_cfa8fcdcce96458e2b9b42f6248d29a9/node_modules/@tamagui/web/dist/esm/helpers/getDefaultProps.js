import { getConfig } from "../config";
const getDefaultProps = (staticConfig, propsComponentName) => {
  let defaultProps = staticConfig?.defaultProps;
  const conf = getConfig(), name = propsComponentName || staticConfig?.componentName || // important: this is how we end up getting the defaultProps we set in createTamagui
  (staticConfig.isText ? "Text" : "View"), userDefaultProps = conf?.defaultProps?.[name];
  return userDefaultProps && (defaultProps = defaultProps ? { ...userDefaultProps, ...defaultProps } : userDefaultProps), defaultProps;
};
export {
  getDefaultProps
};
//# sourceMappingURL=getDefaultProps.js.map
